# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# isolated implementation worktrees
.worktrees/

# logs
npm-debug.log*
pnpm-debug.log*

# environment variables — deny every .env* by default, then re-allow the
# committed template. `.env` alone would NOT have covered `.env.deploy`, which
# holds live FTP credentials.
.env
.env.*
!.env.example

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
